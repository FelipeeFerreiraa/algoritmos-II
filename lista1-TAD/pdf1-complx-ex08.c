#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int n;
    printf("Digite um numero N: ");
    scanf("%d", &n);

    // ex8-letraA------------------------------------
    for (int x = 0; x < 5 * n; x++)
    {
        for (int y = 0; y < n; y++)
        {
            for (int w = 0; w < n; w++)
            {
            }
        }
    }

    return 0;
}